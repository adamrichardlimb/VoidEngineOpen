package renderEngine;

/**
 * Abstract class containing the Shader information.
 *
 * In the future, maybe make a customisable shader so people can do all manner of settings and have them read in and
 * converted to some basic shader code? To be honest, it's probably not worth it.
 */
public abstract class Shader {

    public String shaderCode;

}
